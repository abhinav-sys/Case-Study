server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API calls are made directly from the browser using REACT_APP_API_URL
    # No proxy needed in Railway deployment
}

